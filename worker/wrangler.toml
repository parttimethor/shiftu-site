name = "shiftu-console"
main = "shiftu-console.js"
compatibility_date = "2026-01-01"

# Set the live site origin so the browser only accepts responses from your site.
# Use "*" while testing locally, then lock it down.
[vars]
ALLOWED_ORIGIN = "https://parttimethor.github.io"

# ANTHROPIC_API_KEY is a SECRET, not set here. Add it with:
#   npx wrangler secret put ANTHROPIC_API_KEY
